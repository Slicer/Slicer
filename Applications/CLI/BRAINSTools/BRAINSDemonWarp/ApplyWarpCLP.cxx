#include <iostream>

// main function built in BRAINSResamplePrimary.cxx so that testing only builds
// templates once.

int main(int argc, char *argv[])
{
  std::cout << "DEPRECATED:" << argv[0] << std::endl;
  std::cout << "DEPRECATED:" << argv[0] << std::endl;
  std::cout << "DEPRECATED:" << argv[0] << std::endl;
  std::cout << "DEPRECATED:  You should use BRAINSResample instead." << std::endl;
  std::cout << "DEPRECATED:  BRAINSResample has the same command line." << std::endl;
  std::cout << "DEPRECATED:" << argv[0] << std::endl;
  std::cout << "DEPRECATED:" << argv[0] << std::endl;
  std::cout << "DEPRECATED:" << argv[0] << std::endl;

  return BRAINSResamplePrimary(argc, argv);
}
