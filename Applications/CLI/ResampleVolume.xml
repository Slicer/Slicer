<?xml version="1.0" encoding="utf-8"?>
<executable>
  <category>Filtering</category>
  <title>Resample Volume</title>
  <description>
Resampling an image is a very important task in image analysis.It is
especially important in the frame of image registration. This module
implements image resampling through the use of itk Transforms. This
module uses an Identity Transform. The resampling is controlled by the
Output Spacing. "Resampling" is performed in space coordinates, not
pixel/grid coordinates. It is quite important to ensure that image
spacing is properly set on the images involved. The interpolator is
required since the mapping from one space to the other will often
require evaluation of the intensity of the image at non-grid
positions. Two interpolators are avaliable: linear and sinc. The sinc
interpolator, although more precise, is much slower than the liner
interpolator.
</description>
  <version>0.1.0.$Revision$(alpha)</version>
  <documentation-url></documentation-url>
  <license></license>
  <contributor>Bill Lorensen</contributor>
<acknowledgements>
This work is part of the National Alliance for Medical Image Computing (NAMIC), funded by the National Institutes of Health through the NIH Roadmap for Medical Research, Grant U54 EB005149.
</acknowledgements>

  <parameters>
    <label>Resampling Parameters</label>
    <description>Parameters used for resampling</description>

    <float-vector>
      <name>outputPixelSpacing</name>
      <flag>-s</flag>
      <longflag>--spacing</longflag>
      <description>Spacing along each dimension (0 means use input spacing)</description>
      <label>Spacing</label>
      <default>0,0,0</default>
    </float-vector>

  <string-enumeration>
      <name>interpolationType</name>
      <flag>-i</flag>
      <longflag>--interpolation</longflag>
      <description>Sampling algorithm (linear or sinc)</description>
      <label>Interpolation</label>
      <element>linear</element>
      <element>sinc</element>
      <default>linear</default>
  </string-enumeration>
  </parameters>

  <parameters>
    <label>IO</label>
    <description>Input/output parameters</description>
    <image>
      <name>InputVolume</name>
      <label>Input Volume</label>
      <channel>input</channel>
      <index>0</index>
      <description>Input volume to be resampled</description>
    </image>
    <image>
      <name>OutputVolume</name>
      <label>Output Volume</label>
      <channel>output</channel>
      <index>1</index>
      <description>Resampled Volume</description>
    </image>
  </parameters>
</executable>
