PROJECT(ITKCommandIO)

SET(ITKCOMMANDIO_SRCS
  itkCommandIO.cxx
  itkCommandIOFactory.cxx
)

# Add ITKIO library
ADD_LIBRARY(ITKCommandIO ${ITKCOMMANDIO_SRCS})


TARGET_LINK_LIBRARIES(ITKCommandIO
ITKCommon
slicerio
ITKNrrdIO)

IF(BUILD_SHARED_LIBS)
  INSTALL_TARGETS(${SLICER3_LIB_INSTALL_DIR} ITKCommandIO)
ENDIF(BUILD_SHARED_LIBS)
