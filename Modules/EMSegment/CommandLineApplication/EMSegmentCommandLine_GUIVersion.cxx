//
// This file exists because the command line parsing library requires
// that the xml parameter description file have the same file name
// (modulo extension) as the cxx file.  Since we don't want to
// duplicate implementations, and links don't work well cross
// platform, we use this trick.  Got a better idea?
//

#ifndef EMSegmentCommandLine_GUIVersion_cxx
#define EMSegmentCommandLine_GUIVersion_cxx

#include "EMSegmentCommandLine.cxx"

#endif
